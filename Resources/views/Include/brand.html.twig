<div class="navbar-brand">
    {% if theme_logo() %}
        <img src="{{ asset(theme_logo()) }}">
    {% else %}
        <span class="hidden-folded inline">{{ theme_name() }}</span>
    {% endif %}
</div>
